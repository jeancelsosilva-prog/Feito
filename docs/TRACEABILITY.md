# Tabela de QA — rastreabilidade requisito → implementação → teste

| Requisito | Arquivo responsável | Como foi implementado | Como testar | Limitações conhecidas |
|---|---|---|---|---|
| Registrar início de lavagem em poucos segundos | `frontend/js/ui/taskSheet.js`, `backend/worker/src/handlers/tasks.js` (`POST /api/tasks`) | Bottom sheet com padrão "Agora" pré-selecionado e chips de 1 toque para todos os campos | `tests/api/api.test.mjs` (criação de tarefa); manual: checklist iPhone item 3 | — |
| Lembretes continuam após o usuário apagar a notificação, até conclusão explícita | `backend/worker/src/lib/reminderEngine.js`, `backend/worker/src/cron.js` | Estado vive no D1, não no navegador; Cron Trigger por minuto reprocessa tarefas com `next_reminder_at` vencido independente de notificações anteriores terem sido dispensadas | `tests/unit/reminderEngine.test.mjs`; manual: checklist item 3 | Horário é estimado (granularidade de 1 min do cron + latência do push service) |
| Encerrar envio somente ao abrir o app, entrar na tarefa e confirmar | `frontend/js/ui/today.js` (`completeTask`), `backend/worker/src/handlers/tasks.js` (`handleCompleteTask`) | Botão "Roupa retirada" chama `POST /api/tasks/:id/complete`; cron nunca mais seleciona a tarefa pois `status` sai de `scheduled/reminding` | `tests/api/api.test.mjs` ("tarefa concluída some da lista de ativas"); `tests/unit/reminderEngine.test.mjs` ("tarefa concluída não gera mais notificações") | — |
| Notificação não tem botão "Concluir" embutido; toque abre a tarefa | `frontend/sw.js` (`notificationclick`) | `showNotification` só recebe `data.taskId`, sem `actions`; clique foca/abre janela na tarefa | Manual: checklist item 3 ("Toquei na notificação. O app abriu direto na tarefa") | — |
| Cancelamento secundário, com confirmação | `frontend/js/ui/today.js` (`cancelTask`), `frontend/js/ui/components.js` (`confirmDialog`) | Link discreto abaixo do botão principal; `confirmDialog()` bloqueia a ação até confirmação explícita | `tests/api/api.test.mjs` (fluxo de cancelamento); manual: checklist item 5 | — |
| Frontend estático, estrutura modular (não um único HTML) | `frontend/` (index.html, styles.css, js/\*\*) | Módulos ES (`import`/`export`) organizados em `js/`, `js/ui/`, `js/modules/`, `js/utils/` | `node --check` em todos os arquivos (rodado durante o desenvolvimento); `node tests/run-all.mjs` | — |
| Funciona em subdiretório do GitHub Pages | `frontend/manifest.webmanifest` (`start_url`/`scope`: `"./"`), `frontend/sw.js` (`APP_SHELL` com caminhos `./`), `frontend/sw.js` (`notificationclick` usa `self.registration.scope`) | Nenhum caminho absoluto (`/algo`) é usado — tudo relativo à localização do próprio arquivo | Manual: publicar em `usuario.github.io/nome-repo/` e conferir que ícones, SW e navegação funcionam (checklist item 0–2) | — |
| Backend Cloudflare Worker + D1 + Cron por minuto | `backend/worker/src/index.js`, `wrangler.toml` (`[triggers] crons`) | `scheduled()` roteia por `event.cron`: `"* * * * *"` chama `runReminderSweep()`, `"0 * * * *"` chama `runMaintenance()`; D1 acessado via binding `DB` | `tests/api/api.test.mjs`, `tests/unit/reminderEngine.test.mjs`, `tests/unit/cron.test.mjs` (D1 real via `node:sqlite`); manual: `wrangler tail` durante um teste real | Cron tem granularidade mínima de 1 min (limite da própria plataforma) |
| Web Push com VAPID, sem depender de libs Node incompatíveis com Workers | `backend/worker/src/lib/vapid.js`, `backend/worker/src/lib/webpush.js` | JWT ES256 e criptografia RFC 8291 (aes128gcm) implementados só com Web Crypto API | `tests/unit/vapid.test.mjs` (verificação de assinatura independente), `tests/unit/webpush.test.mjs` (round-trip de criptografia com decifrador independente em Node `crypto`) | — |
| Secret da chave VAPID nunca no frontend | `backend/worker/wrangler.toml` (só a chave pública), `backend/worker/.dev.vars.example`, `.gitignore` | Chave privada cadastrada via `wrangler secret put`; `.dev.vars` no `.gitignore` | Inspeção manual: `grep -r VAPID_PRIVATE frontend/` não deve retornar nada | — |
| CORS restrito ao domínio do GitHub Pages | `backend/worker/src/lib/cors.js` | Só ecoa `Access-Control-Allow-Origin` se a origem da requisição estiver em `ALLOWED_ORIGINS` | `tests/api/api.test.mjs` (preflight de origem permitida vs. não permitida) | — |
| Validação de payloads | `backend/worker/src/lib/validate.js` | Cada campo de `POST /api/tasks` e `POST /api/push/subscribe` é validado (enum, formato de hora, datas) antes de tocar no banco | `tests/api/api.test.mjs`; inspeção de código | — |
| Autenticação por instalação (token), sem permitir assumir installationId alheio | `backend/worker/src/lib/auth.js`, `backend/worker/src/handlers/installations.js` | Token de 32 bytes gerado no servidor; só o hash SHA-256 é armazenado; requisições exigem `X-Installation-Id` + `Authorization: Bearer` correspondentes | `tests/api/api.test.mjs` ("token errado -> 401") | — |
| Proteção contra repetição de requisições (idempotência) | `backend/worker/src/lib/idempotency.js`, header `Idempotency-Key` | Resposta da primeira execução é salva e devolvida em replays com a mesma chave; reuso da chave com payload/recurso diferente retorna 409 em vez de devolver a resposta errada (ver linha específica de idempotência mais abaixo) | `tests/api/api.test.mjs` ("replay da mesma Idempotency-Key -> mesma tarefa, sem duplicar") | Chaves de idempotência expiram após 7 dias (limpeza automática) |
| Rate limiting básico | `backend/worker/src/lib/rateLimit.js` | Janela deslizante por `(installationId, rota)` armazenada no D1 | Inspeção de código; `POST /api/push/test` limitado a 3 a cada 5 min | Não protege contra abuso distribuído entre muitas instalações forjadas — suficiente para o volume do MVP |
| Logs sem dados sensíveis | `backend/worker/src/lib/response.js` (`errorResponse`), `reminderEngine.js` | Erros 500 nunca vazam stack/detalhes ao cliente; logs de servidor não incluem tokens nem chaves de assinatura completas | Inspeção de código | — |
| Nenhum recurso simulado/visual-only | Todo `backend/worker/src/` + `frontend/js/` | Toda ação de UI (iniciar, concluir, cancelar, pausar, retomar, home-boost, apagar dados) chama um endpoint real e reage à resposta real | `node tests/run-all.mjs` — 8 arquivos de teste, ~120 verificações passando contra código real (handlers reais, D1 real em memória, criptografia real), não mocks de UI | — |
| Detecção de plataforma (Safari, standalone, não suportado, permissões) | `frontend/js/platform.js` | Feature detection (`navigator.standalone`, `matchMedia('(display-mode: standalone)')`, `Notification.permission`, etc.) | Manual: checklist itens 1, 2, 7 | Sem API dedicada para "é Safari" — usa checagem de User-Agent como último recurso, documentado no próprio código |
| Tela de instalação (Safari, não instalado) | `frontend/index.html` (`#screen-install`), `frontend/js/ui/onboarding.js`, `frontend/js/app.js` | Mostrada quando `isIos && !isStandalone`; nunca pede permissão de notificação nesta tela | Manual: checklist item 1 | — |
| Tela "Ativar lembretes" separada, com `requestPermission()` só no toque | `frontend/index.html` (`#screen-enable-notifications`), `frontend/js/ui/onboarding.js`, `frontend/js/push.js` (`activateNotifications`) | `Notification.requestPermission()` chamado dentro do handler de `onclick`, nunca no load | Manual: checklist item 2 | — |
| Notificação de teste após autorizar | `frontend/js/push.js` (`activateNotifications`), `backend/worker/src/handlers/push.js` (`handlePushTest`) | Após assinar com sucesso, o cliente chama `POST /api/push/test` automaticamente | Manual: checklist item 2 | Limitado a 3 envios de teste a cada 5 min |
| Instruções para reativar permissão negada, sem modais repetitivos | `frontend/js/ui/onboarding.js`, `frontend/js/ui/settings.js` | Mensagem estática de texto ("Ajustes do iPhone → Feito? → Notificações"), nunca um modal reaberto automaticamente | Manual: checklist item 7 | — |
| Badging API com feature detection | `frontend/js/platform.js` (`supportsBadging`), `frontend/js/app.js` (`updateBadge`), `frontend/sw.js` (evento `push`) | `'setAppBadge' in navigator` checado antes de qualquer chamada | Manual: checklist item 11 (badge some ao apagar dados) | Nem todo navegador suporta — app não quebra onde não há suporte |
| Três áreas de navegação (Hoje/Histórico/Ajustes) | `frontend/index.html` (`.tab-bar`), `frontend/js/app.js` (`wireTabBar`) | Troca de painel via `hidden` + classe `is-active`, sem framework de rotas | Manual: navegação entre as 3 abas | — |
| Cartão de tarefa ativa com todos os campos exigidos | `frontend/js/ui/today.js` (`renderTaskCard`) | Grid com início, tempo decorrido (atualizado a cada 30s), primeiro aviso, próximo aviso, contagem de avisos, status | Manual: checklist item 3 | — |
| Conclusão como ação visualmente dominante, sem celebração exagerada | `frontend/styles.css` (`.btn-complete`), `frontend/js/ui/today.js` | Botão grande, verde, único da cor no cartão; mensagem de texto simples, sem confete/som | Revisão visual manual | — |
| `prefers-reduced-motion` respeitado | `frontend/styles.css` (bloco `@media (prefers-reduced-motion: reduce)`) | Zera durações de transição/animação globalmente | Manual: ativar "Reduzir Movimento" nos Ajustes de Acessibilidade do iPhone | — |
| Bottom sheet "Iniciar lavagem" com todos os campos da seção 5 | `frontend/js/ui/taskSheet.js` | Tipo, horário de início, chips de primeiro aviso, chips de repetição, intensidade, horário silencioso, resumo em linguagem humana | Manual: checklist item 3 | — |
| Resumo humano antes de confirmar | `frontend/js/ui/taskSheet.js` (`summaryText`) | Frase gerada dinamicamente a partir do estado do formulário | Manual: observar o texto no sheet antes de tocar em "Começar acompanhamento" | — |
| Chips do sheet refletem visualmente a seleção atual (primeiro aviso e repetição) | `frontend/js/ui/taskSheet.js` (`rerender`) | Qualquer mudança que afete qual chip aparece selecionado dispara uma re-renderização completa do formulário, não só do texto do resumo | Manual: escolher outro valor de "primeiro aviso"/"repetição" no sheet e confirmar visualmente qual chip fica destacado | Encontrado e corrigido durante revisão de QA — antes, só o texto do resumo era atualizado e o chip antigo continuava marcado |
| Repetição personalizada (1–240 min), além dos chips fixos | `frontend/js/ui/taskSheet.js` (chip "Personalizar" + campo numérico) | Estado `repeatCustomMinutes` tem prioridade sobre `repeatMinutes` quando preenchido; mesmo limite aceito pelo backend | Manual: escolher "Personalizar" e digitar um valor como 2 ou 45 minutos | — |
| Sanitização do título personalizado também no frontend, antes do envio | `frontend/js/ui/taskSheet.js` (`validate()` chama `sanitizePlainText`), `frontend/js/utils/sanitize.js` | O payload enviado ao backend já vem limpo; o backend (`validate.js` `sanitizeText`) sanitiza de novo de qualquer forma, por não confiar no cliente | Inspeção de código; `tests/api/api.test.mjs` cobre o caminho do servidor | Encontrado durante revisão de QA — a função existia mas não era chamada |
| Validação de datas incoerentes já na interface (antes de chamar a API) | `frontend/js/ui/taskSheet.js` (`validate()`) | Bloqueia envio se o primeiro aviso for antes do início, se o intervalo de repetição estiver fora de 1–240 min, ou se o horário silencioso estiver incompleto — mensagem de erro aparece perto do botão de envio | Manual: tentar configurar um primeiro aviso antes do início e confirmar que o app impede o envio com uma mensagem clara | O backend valida de novo (é a fonte da verdade); a validação do frontend é só para dar feedback rápido, sem round-trip |
| Estados da tarefa (`scheduled/reminding/completed/cancelled/paused`) | `backend/migrations/0001_init.sql` (coluna `status`), `backend/worker/src/handlers/tasks.js` | Transições controladas por endpoint dedicado, nunca por escrita direta arbitrária | `tests/api/api.test.mjs` | — |
| Cron: seleção de mensagem sem repetir a última, idempotência, cálculo de próximo horário, invalidação de assinatura morta | `backend/worker/src/lib/reminderEngine.js`, `backend/worker/src/lib/messages.js`, `backend/worker/src/lib/webpush.js` (`PushSendError.isPermanent`) | Ver seção 6 do briefing — implementado passo a passo em `processDueTask()` | `tests/unit/reminderEngine.test.mjs`, `tests/unit/messages.test.mjs` | — |
| Sem `setTimeout()` do navegador como fonte de verdade | `backend/worker/src/cron.js` | Todo agendamento vive no D1 (`next_reminder_at`); o navegador só exibe o que o servidor manda | Manual: fechar o app completamente e ainda assim receber a notificação (checklist item 3) | — |
| Pools de mensagens por estágio, tom descontraído sem copiar identidade de terceiros | `backend/worker/src/lib/messages.js` | 4 pools (`first`, `follow_up`, `later`, `home_boost`) com os textos do briefing, seleção sem repetição consecutiva | `tests/unit/messages.test.mjs` | — |
| Deep link da notificação para a tarefa certa | `frontend/sw.js` (`notificationclick`), `frontend/js/app.js` (`openTaskById`) | URL construída a partir de `self.registration.scope` + `?task=id`; foco de janela existente ou abertura de nova | Manual: checklist item 3 | — |
| Reforço "estou em casa" — experimental, só em primeiro plano, sem geofencing real | `frontend/js/ui/settings.js` (`checkHomeArrivalIfConfigured`, cadastro de casa/raio), `backend/worker/src/handlers/tasks.js` (`handleHomeBoost`) | Coordenadas ficam só no IndexedDB local; ao backend só é enviado o efeito (`home_boost_until`), nunca lat/lng | Manual: botão "Estou em casa" no cartão de tarefa; `tests/api/api.test.mjs` ("POST home-boost -> 200, home_boost_until no futuro"; "home-boost em tarefa já concluída -> 409") | Sem geofencing em segundo plano — limitação inerente de PWA no iOS, documentada na própria UI |
| Design: iOS-like, tipografia do sistema, cores semânticas, cantos 16–24px, `dvh`, `safe-area-inset`, alvo de toque ≥44px, modo escuro | `frontend/styles.css` | Variáveis CSS (`--radius-*`, `--color-*`), `100dvh`, `env(safe-area-inset-*)`, `@media (prefers-color-scheme: dark)` | Revisão visual manual em iPhone real (notch/Dynamic Island) | — |
| Nenhum zoom indevido em campos no iOS | `frontend/styles.css` (`input, select, textarea { font-size: 16px }`) | Fonte de campos nunca menor que 16px | Manual: tocar em um campo de texto no sheet e confirmar que a tela não dá zoom | — |
| CSP e headers de segurança | `frontend/index.html` (`<meta http-equiv="Content-Security-Policy">`) | `script-src 'self'`, `connect-src` restrito à API, sem `unsafe-inline` em script | Inspeção manual do console do Safari (nenhum bloqueio inesperado); revisão de código | Meta tag CSP tem cobertura um pouco menor que headers HTTP reais (GitHub Pages não permite customizar headers) — documentado aqui |
| Sanitização de texto personalizado | `frontend/js/utils/sanitize.js`, `backend/worker/src/lib/validate.js` (`sanitizeText`) | Remove caracteres de controle e `<`/`>`, limita tamanho, em cliente E servidor | Inspeção de código; `tests/api/api.test.mjs` valida o caminho do servidor | — |
| Queries parametrizadas no D1 | Todo `backend/worker/src/handlers/*.js`, `lib/*.js` | 100% das queries usam `.bind()` — nenhuma interpolação de string em SQL | Inspeção de código (`grep -n "prepare(\`"` não deve mostrar concatenação de variáveis) | — |
| Horários em UTC no banco, exibidos em local, horário silencioso com timezone | `backend/migrations/0001_init.sql`, `backend/worker/src/lib/time.js` | Colunas `_at` sempre ISO UTC; `isWithinQuietHours`/`nextQuietHoursEnd` usam `Intl.DateTimeFormat` com o timezone salvo na tarefa | `tests/unit/time.test.mjs` (janela cruzando meia-noite, limites exatos, mudança de fuso via `America/Sao_Paulo`) | — |
| Concorrência: duas execuções do cron sobre a mesma tarefa vencida não duplicam o envio | `backend/worker/src/lib/reminderEngine.js` (claim antes de enviar) | `INSERT` de reivindicação do slot de notificação ocorre ANTES do envio do push, protegido por `UNIQUE` no D1 | `tests/unit/reminderEngine.test.mjs` (duas chamadas concorrentes de `processDueTask()` via `Promise.all` sobre a mesma tarefa) — bug real de corrida foi encontrado e corrigido durante o desenvolvimento graças a este teste | — |
| Concorrência: conclusão simultânea ao envio de um lembrete (`handleCompleteTask()` correndo contra `processDueTask()`) | `backend/worker/src/lib/reminderEngine.js` (recheck de status entre o claim e o envio), `backend/worker/src/handlers/tasks.js` (`transitionTask` idempotente) | Depois de reivindicar o slot, `processDueTask()` relê o status da tarefa antes de chamar `sendWebPush()`; se já estiver `completed`/`cancelled`, aborta o envio. Não fecha 100% da janela (o envio já em trânsito não pode ser cancelado), mas garante que o estado final da tarefa no banco nunca fica incorreto | `tests/unit/completeVsSend.test.mjs` — dispara o handler real de conclusão (via `router.js`) concorrentemente com `processDueTask()` real, 30 rodadas com bancos frescos, e mede a distribuição das duas ordens possíveis | Em parte das rodadas o push já estava em trânsito quando a conclusão foi gravada (o dispositivo pode receber um aviso "atrasado" de uma tarefa recém-concluída) — comportamento esperado de qualquer sistema com envio assíncrono, documentado no próprio teste, não escondido |
| Idempotência não pode ser "enganada" por reuso de chave em contexto diferente | `backend/worker/src/lib/idempotency.js`, `backend/worker/src/lib/hash.js`, `backend/migrations/0002_idempotency_request_hash.sql` | A resposta cacheada por uma `Idempotency-Key` só é reaproveitada se o hash do payload (ou, nas transições de tarefa, o `taskId`) bater com o da primeira chamada; caso contrário, 409 | `tests/api/api.test.mjs` ("reusar a mesma Idempotency-Key com payload DIFERENTE -> 409"; "reusar a mesma chave para uma tarefa DIFERENTE -> 409, não vaza a resposta da primeira") | A chave em si (`idempotency_keys.key`) ainda é única globalmente, não por instalação — teoricamente duas instalações diferentes usando por acaso o mesmo UUID e o mesmo payload poderiam colidir; probabilidade desprezível com UUIDs aleatórios, não reforçado no MVP |
| Falhas do push service (assinatura morta vs. falha passageira) | `backend/worker/src/lib/webpush.js` (`PushSendError.isPermanent`), `backend/worker/src/lib/reminderEngine.js` | Só 404/410 (assinatura não existe mais) zeram `push_subscriptions.is_valid`; 429/500/503 e erros de rede são tratados como transitórios — a assinatura continua válida e a tarefa é reagendada normalmente | `tests/unit/webpushFailures.test.mjs` (201, 404, 410, 429, 500, 503 e erro de rede simulados) | — |
| Expiração de tarefas abandonadas, limpeza de logs antigos, roda de forma confiável (não depende de contador em memória) | `backend/worker/src/cron.js` (`runMaintenance`), `backend/worker/src/index.js` (`scheduled`), `wrangler.toml` (`crons = ["* * * * *", "0 * * * *"]`) | Dois Cron Triggers separados; o handler decide qual rotina rodar olhando `event.cron`, sem estado em memória (um contador `tickCount % 60` foi removido de propósito — um isolate do Worker pode ser reciclado a qualquer momento e zerar esse contador, tornando "1x por hora" não confiável) | `tests/unit/cron.test.mjs` ("event.cron ... aciona a manutenção / a varredura", cada uma isoladamente) | — |
| Histórico com indicadores neutros, sem valor financeiro inventado, e paginação real | `backend/worker/src/handlers/history.js`, `frontend/js/ui/history.js` | Indicadores contam eventos reais (tarefas concluídas antes do 3º aviso, média de avisos) — nenhuma conversão para R$/litros; paginação por cursor de data | `tests/api/api.test.mjs` ("primeira página respeita o limit e devolve nextCursor"; "segunda página traz o restante e não repete itens") | — |
| "Apagar meus dados deste dispositivo" completo | `frontend/js/ui/settings.js` (`resetEverything`) | Cancela tarefas ativas no backend, remove assinatura Push, limpa IndexedDB/localStorage/Cache Storage, limpa badge, reinicia onboarding | Manual: checklist item 11 | — |
| Atualização da PWA sem recarregar no meio de uma edição | `frontend/sw.js` (`message`/`SKIP_WAITING`), `frontend/js/app.js` (`registerServiceWorker`) | `skipWaiting()` só roda quando o usuário toca em "Atualizar agora" — nunca automaticamente | Manual: checklist item 9 | — |
| Testes unitários da lógica de cadência | `tests/unit/reminderEngine.test.mjs`, `tests/unit/time.test.mjs`, `tests/unit/messages.test.mjs`, `tests/unit/cron.test.mjs`, `tests/unit/completeVsSend.test.mjs`, `tests/unit/webpushFailures.test.mjs` | Cobrem intensidade, home boost, horário silencioso, seleção/anti-repetição de mensagem, as duas corridas (cron-vs-cron e conclusão-vs-envio), roteamento dos dois Cron Triggers, e as respostas do push service (sucesso/permanente/transitória/rede) | `node tests/run-all.mjs` | — |
| Testes da API | `tests/api/api.test.mjs`, `tests/api/fakeD1.mjs` | Exercitam os handlers reais contra um D1 real em memória (`node:sqlite`, com todas as migrations aplicadas via `loadFullSchemaSql`), não mocks de resposta. Cobre todos os endpoints da seção 12 do briefing (instalações, push subscribe/unsubscribe/test, tarefas incluindo pause/resume/home-boost/GET por id, histórico paginado), validação de payloads inválidos, JSON malformado e idempotência (replay, payload divergente, reuso entre tarefas diferentes) | `node tests/run-all.mjs` | Não substitui um teste de ponta a ponta com `wrangler dev` real (ver `tests/README.md`) |
